document.getElementById("show-email").insertAdjacentHTML("beforeend", "<%= j render 'show', email: @email %>")

<%# button_read = document.getElementById("read_box")

button_read.addEventListener('click', function() {
    if (this.value == "Marquer comme non-lu") {
        this.value = "Marquer comme lu"
    } else if (this.value == "{

    }
} %>